# Deprecated

This `docs/` folder is a stale copy of the site assets and is **not** used for deployment. The live site is served from `site/` via the GitHub Actions workflow (`.github/workflows/deploy.yml`).

Do not edit files here -- all changes should be made in `site/` instead.
